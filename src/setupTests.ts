// Import Jest DOM matchers
import '@testing-library/jest-dom';

// Optional: Add any other global setup here, e.g., mocking
// Example:
// import { vi } from 'vitest';
// vi.mock('some-module', () => ({
//   default: vi.fn(),
// })); 